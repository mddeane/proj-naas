import { FocusOffDirective } from './focus-off.directive';

describe('FocusOffDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusOffDirective();
    expect(directive).toBeTruthy();
  });
});
