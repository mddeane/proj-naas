import { DoubleClickOpenDirective } from './double-click-open.directive';

describe('DoubleClickOpenDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubleClickOpenDirective();
    expect(directive).toBeTruthy();
  });
});
